"use strict";
const fs=require("fs"),path=require("path");
const ROOT=process.cwd();
const pad=p=>path.join(ROOT,p);
const lees=p=>fs.readFileSync(pad(p),"utf8");
const schrijf=(p,s)=>{fs.mkdirSync(path.dirname(pad(p)),{recursive:true});fs.writeFileSync(pad(p),s,"utf8");};
function vervang(p,voor,na,aantal=1){
  let s=lees(p),gevonden=s.split(voor).length-1;
  if(gevonden!==aantal) throw new Error(`${p}: verwacht ${aantal}× fragment, vond ${gevonden}`);
  s=s.split(voor).join(na);schrijf(p,s);
}

vervang("lib/waarschuwingen.cjs",
`  [13.2, 15.4, 144.6, 146.1]     // Guam en Noordelijke Marianen\n];`,
`  [13.2, 15.4, 144.6, 146.1],    // Guam en Noordelijke Marianen\n  [-14.6, -10.5, -171.5, -167.0]  // American Samoa (WSO Pago Pago)\n];`);
vervang("lib/waarschuwingen.cjs",
`const UA = "Weerbriefing/1.0 (weerbriefing app; contact via github.com/Tommjoness/weathernow)";`,
`const UA = "WatIsHetWeer/1.0 (watishetweer.nl; contact via github.com/Tommjoness/weathernow)";`);
vervang("lib/waarschuwingen.cjs",
`/* ---------- National Weather Service ---------- */\n\nasync function viaNWS(lat, lon) {`,
`/* ---------- National Weather Service ---------- */\n\n// Houd veiligheidsinformatie leesbaar: nooit midden in een woord afbreken.\n// Bij lange teksten heeft een volledige zin de voorkeur; anders een nette\n// woordgrens met ellipsis. De limiet voorkomt dat één alert de hele pagina vult.\nfunction waarschuwingTekst(waarde, max = 700) {\n  const s = String(waarde || "").replace(/\\s+/g, " ").trim();\n  if (s.length <= max) return s;\n  const stuk = s.slice(0, max + 1);\n  const grenzen = [stuk.lastIndexOf(". "), stuk.lastIndexOf("! "), stuk.lastIndexOf("? ")];\n  const zin = Math.max(...grenzen);\n  if (zin >= Math.min(240, Math.floor(max * 0.55))) return stuk.slice(0, zin + 1).trim();\n  const woord = stuk.slice(0, max).replace(/\\s+\\S*$/, "").trim();\n  return (woord || stuk.slice(0, max).trim()) + "…";\n}\n\nasync function viaNWS(lat, lon) {`);
vervang("lib/waarschuwingen.cjs",
`      tekst: String(i.description || i.instruction || "").replace(/\\s+/g, " ").trim().slice(0, 300),`,
`      tekst: waarschuwingTekst(i.description || i.instruction || ""),`,2);
vervang("lib/waarschuwingen.cjs",
`      tot: i.expires || i.ends || null,`,
`      // NWS: ends is het verwachte einde van het weergebeuren; expires is\n      // de levensduur van dit CAP-bericht en kan dus eerder vallen.\n      tot: i.ends || i.expires || null,`);
vervang("lib/waarschuwingen.cjs",
`      tekst: xmlTekst(s).replace(/\\s+/g, " ").trim().slice(0, 300),`,
`      tekst: waarschuwingTekst(xmlTekst(s)),`);

vervang("lib/plaatsnaam.cjs",
`headers: { "User-Agent": "Weerbriefing/1.0 (github.com/Tommjoness/weathernow)", "Accept": "application/json" },`,
`headers: { "User-Agent": "WatIsHetWeer/1.0 (watishetweer.nl; github.com/Tommjoness/weathernow)", "Accept": "application/json" },`);
vervang("lib/plaatsnaam.cjs",
`  for (const zoek of [viaNominatim, viaBigDataCloud]) {`,
`  // BigDataCloud eerst. De publieke OSMF-Nominatimdienst is bewust alleen\n  // fallback: die heeft een applicatiebrede limiet van 1 request per seconde.\n  for (const zoek of [viaBigDataCloud, viaNominatim]) {`);

vervang("index.html",`<title>Weerbriefing</title>`,`<title>Wat is het weer?</title>`);
vervang("index.html",
`<meta name="description" content="Weersverwachting met kwartierneerslag, spreidingsband, luchtkwaliteit en nachtzicht.">`,
`<meta name="description" content="Actuele weersverwachting met neerslag, weekverwachting, luchtkwaliteit en nachtzicht.">`);
vervang("index.html",`<meta name="apple-mobile-web-app-title" content="Weerbriefing">`,`<meta name="apple-mobile-web-app-title" content="Wat is het weer?">`);
vervang("index.html",`      <h1>Weerbriefing</h1>`,`      <h1>Wat is het weer?</h1>`);
vervang("index.html",`  document.title=S.label+" · Weerbriefing";`,`  document.title=S.label+" · Wat is het weer?";`);
vervang("index.html",
`  .chip.add:hover{border-style:solid}`,
`  .chip.add:hover{border-style:solid}\n  /* Bewaarde plaats en verwijderen zijn twee echte knoppen. Zo is de × ook\n     via toetsenbord en screenreader zelfstandig bereikbaar, zonder geneste\n     interactieve rollen. */\n  .chipplaats,.chip .x{background:transparent;border:0;margin:0;padding:0;font:inherit;letter-spacing:normal;text-transform:none}\n  .chipplaats{color:inherit;cursor:pointer;min-width:0;text-align:left}\n  .chip .x{cursor:pointer;color:var(--ink-25);font-size:15px;line-height:1;padding:0 3px}\n  .chip.add{text-transform:none;letter-spacing:normal;font-weight:400}`);
vervang("index.html",
`    <h2><span>Neerslag komende twee uur</span></h2>\n    <div id="nctext"></div>`,
`    <h2><span>Neerslag komende twee uur</span></h2>\n    <p class="hint" id="nchint">Modelverwachting per kwartier. De bronresolutie verschilt per regio; buiten gebieden met echte 15-minutenmodeldata kan Open-Meteo uurdata interpoleren.</p>\n    <div id="nctext"></div>`);
vervang("index.html",
`    <footer>\n      <span class="bron"><b>Weer</b> <a href="https://open-meteo.com" target="_blank" rel="noopener">Open-Meteo Best Match</a> · <b>Luchtkwaliteit</b> CAMS</span>\n      <span class="mono bron" id="coords"></span>\n    </footer>`,
`    <footer>\n      <span class="bron"><b>Weer en plaatszoeken</b> <a href="https://open-meteo.com" target="_blank" rel="noopener">Open-Meteo</a> · <b>Luchtkwaliteit en pollen</b> Open-Meteo / CAMS</span>\n      <span class="bron"><b>Waarschuwingen</b> <a href="https://meteoalarm.org" target="_blank" rel="noopener">MeteoAlarm</a> / <a href="https://www.weather.gov" target="_blank" rel="noopener">National Weather Service</a> · <b>Plaatsnamen</b> BigDataCloud / <a href="https://www.openstreetmap.org/copyright" target="_blank" rel="noopener">© OpenStreetMap-bijdragers</a> (fallback)</span>\n      <span class="mono bron" id="coords"></span>\n      <span class="bron"><a href="privacy.html">Privacy &amp; gegevens</a> · Geen account, advertentietracking of analytics.</span>\n    </footer>`);
vervang("index.html",
`const ls={\n  get(k,f){try{const v=localStorage.getItem(k);return v?JSON.parse(v):f;}catch(e){return f;}},\n  set(k,v){try{localStorage.setItem(k,JSON.stringify(v));}catch(e){}}\n};`,
`const ls={\n  get(k,f){try{const v=localStorage.getItem(k);return v?JSON.parse(v):f;}catch(e){return f;}},\n  set(k,v){try{localStorage.setItem(k,JSON.stringify(v));}catch(e){}}\n};\n// Lokale opslag en deel-URL's hebben geen GPS-precisie nodig. Drie decimalen\n// is ongeveer honderd meter en beperkt onnodige locatieprecisie in blijvende data.\nconst coordOpslag=v=>Number(Number(v).toFixed(3));`);
vervang("index.html",
`    if(opslaan) ls.set(KEY_P,{lat:S.lat,lon:S.lon,label:label});\n    ls.set(KEY_D,{d:S.d,air:null,label:label,lat:S.lat,lon:S.lon,op:S.op});`,
`    if(opslaan) ls.set(KEY_P,{lat:coordOpslag(S.lat),lon:coordOpslag(S.lon),label:label});\n    ls.set(KEY_D,{d:S.d,air:null,label:label,lat:coordOpslag(S.lat),lon:coordOpslag(S.lon),op:S.op});`);
vervang("index.html",
`      ls.set(KEY_D,{d:S.d,air:S.air,label:S.label,lat:S.lat,lon:S.lon,op:S.op});`,
`      ls.set(KEY_D,{d:S.d,air:S.air,label:S.label,lat:coordOpslag(S.lat),lon:coordOpslag(S.lon),op:S.op});`);
vervang("index.html",
`        ls.set(KEY_P,{lat:S.lat,lon:S.lon,label:S.label});`,
`        ls.set(KEY_P,{lat:coordOpslag(S.lat),lon:coordOpslag(S.lon),label:S.label});`);
vervang("index.html",
`      l.push({lat:S.lat,lon:S.lon,label:S.label});ls.set(KEY_L,l);chips();`,
`      l.push({lat:coordOpslag(S.lat),lon:coordOpslag(S.lon),label:S.label});ls.set(KEY_L,l);chips();`);
vervang("index.html",
`    u.searchParams.set("lat",S.lat.toFixed(4));u.searchParams.set("lon",S.lon.toFixed(4));`,
`    u.searchParams.set("lat",S.lat.toFixed(3));u.searchParams.set("lon",S.lon.toFixed(3));`);
vervang("index.html",
`/* ---------- officiele waarschuwingen ---------- */\nasync function waarschuwingen(){`,
`/* ---------- officiele waarschuwingen ---------- */\nfunction waarschuwingGeldigTot(iso){\n  const ms=Date.parse(String(iso||""));\n  if(!Number.isFinite(ms)) return null;\n  const tz=S.d&&S.d.timezone;\n  const p=tz?weatherNowZoneDelen(ms,tz):null;\n  if(!p) return null;\n  const datum=p.year+"-"+String(p.month).padStart(2,"0")+"-"+String(p.day).padStart(2,"0");\n  const tijd=String(p.hour).padStart(2,"0")+":"+String(p.minute).padStart(2,"0");\n  const vandaag=plaatsVandaag(),nu=plaatsTijdDelen();\n  const morgen=new Date(Date.UTC(nu.year,nu.month-1,nu.day)+86400000).toISOString().slice(0,10);\n  if(datum===vandaag) return "vandaag "+tijd;\n  if(datum===morgen) return "morgen "+tijd;\n  try{\n    return new Intl.DateTimeFormat("nl-NL",{timeZone:tz,weekday:"short",day:"numeric",month:"short",hour:"2-digit",minute:"2-digit",hourCycle:"h23"}).format(new Date(ms));\n  }catch(e){ return datum+" "+tijd; }\n}\nasync function waarschuwingen(){`);
vervang("index.html",
`    el.innerHTML=lijst.slice(0,3).map(w=>\n      \`<div class="waarsch"><h3>\${esc(w.titel)}</h3><p>\${esc(w.tekst||"")}\`\n      +(w.tot?" Geldig tot "+esc(w.tot)+".":"")\n      +(w.landelijk?" Geldt voor een groter gebied, niet per se voor deze plaats.":"")\n      +\`</p></div>\`).join("");`,
`    el.innerHTML=lijst.slice(0,3).map(w=>{\n      const geldig=w.tot?waarschuwingGeldigTot(w.tot):null;\n      return \`<div class="waarsch"><h3>\${esc(w.titel)}</h3><p>\${esc(w.tekst||"")}\`\n        +(geldig?" Geldig tot "+esc(geldig)+".":"")\n        +(w.landelijk?" Geldt voor een groter gebied, niet per se voor deze plaats.":"")\n        +\`</p></div>\`;\n    }).join("");`);
vervang("index.html",
`  let rij=lijst.map((p,i)=>\`<span class="chip\${zelfde(p)?" on":""}" data-i="\${i}" role="button" tabindex="0" title="Toon het weer voor \${esc(p.label)}">\${esc(p.label)}<span class="x" data-x="\${i}" title="Verwijderen">×</span></span>\`).join("");\n  if(!lijst.some(zelfde)&&S.lat!=null) rij+=\`<span class="chip add" id="chipadd" role="button" tabindex="0" title="Voeg deze plaats toe aan je lijst">+ \${esc(S.label)} bewaren</span>\`;`,
`  let rij=lijst.map((p,i)=>\`<span class="chip\${zelfde(p)?" on":""}"><button type="button" class="chipplaats" data-i="\${i}" title="Toon het weer voor \${esc(p.label)}">\${esc(p.label)}</button><button type="button" class="x" data-x="\${i}" title="Verwijderen" aria-label="Verwijder \${esc(p.label)} uit bewaarde plaatsen">×</button></span>\`).join("");\n  if(!lijst.some(zelfde)&&S.lat!=null) rij+=\`<button type="button" class="chip add" id="chipadd" title="Voeg deze plaats toe aan je lijst">+ \${esc(S.label)} bewaren</button>\`;`);
vervang("index.html",
`  c.querySelectorAll(".chip[data-i]").forEach(el=>{\n    const ga=e=>{\n      if(e.target.dataset&&e.target.dataset.x!=null) return;\n      const p=ls.get(KEY_L,[])[+el.dataset.i];\n      document.getElementById("q").value=p.label;load(p.lat,p.lon,p.label);\n    };\n    el.addEventListener("click",ga);\n    el.addEventListener("keydown",e=>{if(e.key==="Enter"||e.key===" "){e.preventDefault();ga(e);}});\n  });`,
`  c.querySelectorAll(".chipplaats[data-i]").forEach(el=>{\n    el.addEventListener("click",()=>{\n      const p=ls.get(KEY_L,[])[+el.dataset.i];\n      document.getElementById("q").value=p.label;load(p.lat,p.lon,p.label);\n    });\n  });`);
vervang("index.html",
`    add.addEventListener("click",bewaar);\n    add.addEventListener("keydown",e=>{if(e.key==="Enter"||e.key===" "){e.preventDefault();bewaar();}});`,
`    add.addEventListener("click",bewaar);`);
vervang("index.html",
`  // C. terugkerende gebruiker: de opgeslagen plaats laadt meteen, en\n  //    tegelijkertijd loopt de actuele locatie op de achtergrond mee\n  const v=ls.get(KEY_P,null);\n  if(v&&v.lat!=null){\n    q.value=v.label;\n    load(v.lat,v.lon,v.label);\n    locatieNu("auto-terugkerend");\n    return;\n  }\n\n  // D. eerste bezoek zonder enige bekende locatie: automatisch gps, nooit\n  //    Almere of een andere gegokte plaats als daar niets uit komt\n  const st0=document.getElementById("state");\n  st0.style.display="block";st0.className="msg";st0.textContent="Locatie bepalen.";\n  locatieNu("auto-leeg").then(ok=>{\n    if(!ok){\n      st0.style.display="block";st0.className="msg err";\n      st0.textContent="Kon je locatie niet bepalen. Zoek hierboven een plaats, of probeer straks opnieuw op “Mijn locatie”.";\n    }\n  });`,
`  // C. terugkerende gebruiker: laad de laatst gekozen plaats direct. De browser\n  //    krijgt geen nieuwe locatievraag totdat de gebruiker zelf "Mijn locatie" kiest.\n  const v=ls.get(KEY_P,null);\n  if(v&&v.lat!=null){\n    q.value=v.label;\n    load(v.lat,v.lon,v.label);\n    return;\n  }\n\n  // D. eerste bezoek: geen automatische gps-prompt. De gebruiker kiest zelf\n  //    tussen zoeken en "Mijn locatie"; dat is duidelijker en privacyvriendelijker.\n  const st0=document.getElementById("state");\n  st0.style.display="block";st0.className="msg";\n  st0.textContent="Zoek hierboven een plaats of kies ‘Mijn locatie’.";`);

vervang("build-weather.js",
`const versie="weerbriefing-"+crypto.createHash("sha256").update(html).digest("hex").slice(0,12);\nconst swp=path.join(OUT,"sw.js");\nif(fs.existsSync(swp)){\n  let sw=fs.readFileSync(swp,"utf8").replace(/weerbriefing-(?:v\\d+|[0-9a-f]{12})/g,versie);`,
`const CACHE_BRONNEN=[\n  "index.html","manifest.json","icon-192.png","icon-512.png","icon-maskable-512.png",\n  "bodoni-moda-latin-400-normal.woff2","bodoni-moda-latin-500-normal.woff2",\n  "instrument-sans-latin-400-normal.woff2","instrument-sans-latin-500-normal.woff2",\n  "instrument-sans-latin-600-normal.woff2","dm-mono-latin-400-normal.woff2","dm-mono-latin-500-normal.woff2"\n];\nconst cacheHash=crypto.createHash("sha256");\nfor(const naam of CACHE_BRONNEN){\n  const p=path.join(OUT,naam);\n  if(!fs.existsSync(p)) throw new Error("App-shellbestand ontbreekt voor cachehash: "+naam);\n  cacheHash.update(naam+"\\0");cacheHash.update(fs.readFileSync(p));cacheHash.update("\\0");\n}\nconst versie="watishetweer-"+cacheHash.digest("hex").slice(0,12);\nconst swp=path.join(OUT,"sw.js");\nif(fs.existsSync(swp)){\n  let sw=fs.readFileSync(swp,"utf8").replace(/(?:weerbriefing|watishetweer)-(?:v\\d+|[0-9a-f]{12})/g,versie);`);
vervang("sw.js",`const CACHE = "weerbriefing-v72";`,`const CACHE = "watishetweer-v1";`);

const manifest=JSON.parse(lees("manifest.json"));
manifest.name="Wat is het weer?";
manifest.short_name="Weer";
manifest.description="Actuele weersverwachting met neerslag, weekverwachting, luchtkwaliteit en nachtzicht.";
schrijf("manifest.json",JSON.stringify(manifest,null,2)+"\n");
vervang("audit-regressions.test.js",
`ok(/const CACHE = "weerbriefing-[0-9a-f]{12}";/.test(sw),"serviceworker-cache volgt de gebouwde inhoudshash");\nok(!/weerbriefing-v\\d+/.test(sw),"geen handmatig vast cacheversienummer in productie");`,
`ok(/const CACHE = "watishetweer-[0-9a-f]{12}";/.test(sw),"serviceworker-cache volgt de gebouwde inhoudshash");\nok(!/(?:weerbriefing|watishetweer)-v\\d+/.test(sw),"geen handmatig vast cacheversienummer in productie");`);

schrijf("privacy.html",`<!DOCTYPE html>\n<html lang="nl">\n<head>\n<meta charset="utf-8">\n<meta name="viewport" content="width=device-width, initial-scale=1">\n<title>Privacy & gegevens · Wat is het weer?</title>\n<meta name="robots" content="index,follow">\n<meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; connect-src 'self'; base-uri 'none'; form-action 'none'">\n<style>body{margin:0;background:#f4f5f3;color:#12211c;font:16px/1.65 system-ui,-apple-system,sans-serif;padding:32px 20px}.kaart{max-width:760px;margin:auto;background:#fff;border:1px solid #dce1de;padding:32px}h1{font:400 36px/1.15 Georgia,serif;margin:0 0 24px}h2{font-size:17px;margin-top:28px}a{color:inherit}button{background:transparent;border:1px solid #41514b;padding:10px 14px;cursor:pointer;font:inherit}.klein{color:#65716c;font-size:14px}#wisstatus{margin-left:10px}</style>\n</head>\n<body><main class="kaart">\n<p><a href="./">← Terug naar het weer</a></p>\n<h1>Privacy & gegevens</h1>\n<p>Wat is het weer? heeft geen account, advertentietracking of analytics. De app gebruikt alleen gegevens die nodig zijn om het weer voor een gekozen plaats te tonen.</p>\n<h2>Je locatie</h2>\n<p>Je browser vraagt pas om je actuele locatie nadat je zelf op <b>Mijn locatie</b> kiest. De coördinaten worden gebruikt voor weerdata, een plaatsnaam en — waar beschikbaar — officiële weerwaarschuwingen. In lokale opslag en in de deel-URL wordt de positie afgerond op drie decimalen (ongeveer honderd meter); tijdens de actuele aanvraag kan de browser nauwkeuriger coördinaten gebruiken.</p>\n<h2>Externe diensten</h2>\n<p>Voor weer, luchtkwaliteit, pollen en plaatszoeken gebruikt de app Open-Meteo. Voor reverse-geocoding gebruikt de server primair BigDataCloud en alleen als fallback OpenStreetMap Nominatim. Officiële waarschuwingen komen, afhankelijk van de locatie, van MeteoAlarm of de Amerikaanse National Weather Service. Deze aanbieders en de hostingprovider kunnen bij een aanvraag technische gegevens zoals IP-adres en aangevraagde locatie verwerken volgens hun eigen voorwaarden.</p>\n<h2>Wat je browser bewaart</h2>\n<p>De laatst gekozen plaats, bewaarde plaatsen, thema-instelling en een recente weersnapshot worden lokaal in je browser opgeslagen zodat de app sneller en ook bij een korte storing bruikbaar blijft. Er is geen eigen gebruikersdatabase waarin deze voorkeuren aan een account worden gekoppeld.</p>\n<h2>Zelf wissen</h2>\n<p><button id="wis" type="button">Wis lokale gegevens</button><span id="wisstatus" role="status" aria-live="polite"></span></p>\n<p class="klein">Dit wist de lokaal opgeslagen plaatsen, instellingen en weersnapshot van deze site. Je kunt sitegegevens ook via de instellingen van je browser verwijderen.</p>\n<h2>Bronnen en contact</h2>\n<p>Bronvermelding staat ook onderaan de weerpagina. Voor vragen over deze app of gegevensverwerking kun je contact opnemen via de <a href="https://github.com/Tommjoness/weathernow">GitHub-repository</a>.</p>\n</main>\n<script>document.getElementById("wis").addEventListener("click",()=>{try{localStorage.clear();document.getElementById("wisstatus").textContent=" Lokale gegevens gewist.";}catch(e){document.getElementById("wisstatus").textContent=" Wissen lukte niet; gebruik de browserinstellingen.";}});</script>\n</body></html>\n`);

schrijf("prelaunch-regressions.test.js",`"use strict";\nconst assert=require("assert"),fs=require("fs"),path=require("path"),cp=require("child_process");\nconst R=__dirname,lees=p=>fs.readFileSync(path.join(R,p),"utf8");let n=0;\nconst ok=(c,m)=>{assert.ok(c,m);n++;console.log("OK  "+m);};\nconst index=lees("index.html"),waars=lees("lib/waarschuwingen.cjs"),plaats=lees("lib/plaatsnaam.cjs"),build=lees("build-weather.js"),workflow=lees(".github/workflows/senior-fix-verification.yml");\nconst manifest=JSON.parse(lees("manifest.json"));\nok(index.includes("Wat is het weer?")&&manifest.name==="Wat is het weer?","publieke merknaam is consequent Nederlands");\nok(index.includes("privacy.html")&&fs.existsSync(path.join(R,"privacy.html")),"privacy-informatie is direct bereikbaar");\nok(!index.includes('locatieNu("auto-terugkerend");')&&!index.includes('locatieNu("auto-leeg").then'),"gps start niet automatisch bij openen");\nok(index.includes("coordOpslag")&&index.includes('S.lat.toFixed(3)')&&index.includes('S.lon.toFixed(3)'),"blijvende/deelbare locatieprecisie is beperkt tot drie decimalen");\nok(waars.includes("i.ends || i.expires || null"),"NWS gebruikt gebeurteniseinde vóór CAP-berichtverval");\nok(waars.includes("American Samoa")&&waars.includes("-14.6"),"American Samoa valt binnen NWS-dekking");\nok(waars.includes("waarschuwingTekst")&&!waars.includes("trim().slice(0, 300)"),"waarschuwingstekst breekt niet meer hard op 300 tekens af");\nok(index.includes("waarschuwingGeldigTot")&&!index.includes('" Geldig tot "+esc(w.tot)'),"waarschuwingstijd wordt lokaal en menselijk geformatteerd");\nok(index.includes("kan Open-Meteo uurdata interpoleren"),"kwartiergrafiek benoemt mogelijke interpolatie");\nok(plaats.indexOf("[viaBigDataCloud, viaNominatim]")>=0,"Nominatim is alleen fallback voor reverse-geocoding");\nok(index.includes("© OpenStreetMap-bijdragers")&&index.includes("MeteoAlarm")&&index.includes("National Weather Service"),"relevante databronnen zijn zichtbaar geattribueerd");\nok(index.includes('type="button" class="x"')&&index.includes('aria-label="Verwijder'),"verwijderen van bewaarde plaats is keyboard- en screenreaderbereikbaar");\nok(build.includes("CACHE_BRONNEN")&&build.includes('"manifest.json"')&&build.includes('"icon-192.png"')&&build.includes('instrument-sans-latin-600-normal.woff2'),"cachehash omvat de volledige app-shell");\nok(workflow.includes("actions/checkout@v6")&&workflow.includes("actions/setup-node@v6")&&workflow.includes("playwright@latest"),"CI gebruikt actuele Node-24 Actions en actuele Playwright");\nfunction nepRes(){return{statusCode:200,headers:{},body:null,setHeader(k,v){this.headers[String(k).toLowerCase()]=v;},status(c){this.statusCode=c;return this;},json(b){this.body=b;return this;}};}\nasync function roep(moduleNaam,query,fetchImpl){const oud=global.fetch,p=require.resolve(moduleNaam);delete require.cache[p];global.fetch=fetchImpl;try{const h=require(p),r=nepRes();await h({query},r);return r;}finally{global.fetch=oud;delete require.cache[p];}}\n(async()=>{\n  const lang="Dit is een volledige eerste zin met belangrijke veiligheidsinformatie. "+"waarschuwing ".repeat(90)+"einde";\n  const ny=await roep("./lib/waarschuwingen.cjs",{lat:"40.7128",lon:"-74.0060"},async url=>({ok:true,json:async()=>({features:[{properties:{event:"Heat Advisory",description:lang,severity:"Moderate",onset:"2026-08-09T10:00:00-04:00",expires:"2026-08-09T11:00:00-04:00",ends:"2026-08-09T19:00:00-04:00",areaDesc:"New York"}}]})}));\n  ok(ny.body.lijst[0].tot==="2026-08-09T19:00:00-04:00","live NWS-adapter bewaart ends als geldigheid");\n  ok((ny.body.lijst[0].tekst.length<=701&&!/\\w$/.test(ny.body.lijst[0].tekst.slice(-1)))||ny.body.lijst[0].tekst.endsWith("."),"lange waarschuwing wordt op nette grens ingekort");\n  let asUrl="";const as=await roep("./lib/waarschuwingen.cjs",{lat:"-14.2756",lon:"-170.7020"},async url=>{asUrl=String(url);return{ok:true,json:async()=>({features:[]})};});\n  ok(as.body.bron==="National Weather Service"&&as.body.dekking===true&&asUrl.includes("api.weather.gov/alerts/active?point="),"American Samoa gebruikt de NWS-puntroute");\n  const urls=[];const pl=await roep("./lib/plaatsnaam.cjs",{lat:"52.35",lon:"5.26"},async url=>{urls.push(String(url));return{ok:true,json:async()=>({city:"Almere"})};});\n  ok(pl.body.naam==="Almere"&&urls.length===1&&urls[0].includes("bigdatacloud"),"BigDataCloud voorkomt normale Nominatim-aanroep");\n  const manifestPad=path.join(R,"manifest.json"),origineel=fs.readFileSync(manifestPad,"utf8"),swPad=path.join(R,"public","sw.js");\n  const cache=()=>{const m=/const CACHE = "([^"]+)";/.exec(fs.readFileSync(swPad,"utf8"));return m&&m[1];};\n  const voor=cache();\n  try{\n    const gewijzigd=JSON.parse(origineel);gewijzigd.short_name="Weer test";fs.writeFileSync(manifestPad,JSON.stringify(gewijzigd,null,2)+"\\n");\n    cp.execFileSync(process.execPath,[path.join(R,"build-weather.js")],{cwd:R,stdio:"pipe"});\n    ok(cache()!==voor,"wijziging van manifest verandert serviceworker-cachehash");\n  }finally{\n    fs.writeFileSync(manifestPad,origineel);\n    cp.execFileSync(process.execPath,[path.join(R,"build-weather.js")],{cwd:R,stdio:"pipe"});\n  }\n  console.log("Pre-launch regressies: "+n+" controles geslaagd.");\n})().catch(e=>{console.error(e&&e.stack||e);process.exit(1);});\n`);

const pkg=JSON.parse(lees("package.json"));
pkg.name="watishetweer";
pkg.description="Nederlandse weersverwachting met centrale interpretatielaag, neerslag, luchtkwaliteit, pollen en nachtzicht.";
for(const k of ["test","build"]){
  if(!pkg.scripts[k].includes("prelaunch-regressions.test.js"))
    pkg.scripts[k]=pkg.scripts[k].replace("node audit-regressions.test.js","node audit-regressions.test.js && node prelaunch-regressions.test.js");
}
schrijf("package.json",JSON.stringify(pkg,null,2)+"\n");

console.log("Pre-launch hardening: 12 auditpunten toegepast.");
